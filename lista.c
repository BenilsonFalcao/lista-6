#include <stdio.h>
/*Faça um programa que leia
um número inteiro x e, em seguida, solicite
ao usuário outros 50 valores inteiros. Ao
final, o programa deve exibir o total de
múltiplos de x fornecidos.*/

int x, n, quantInteiros, divisor
do
{
  print (defina o valor: )
  scanf ("%d" &n)
}  
while (quantInteiros<=50)
  for (quantInteiros=1; quantInteiros<=50; quantInteiros++)

if
  n/x = 0
